# DIST FUNCTIONs ---------------------------------------------------------------------------------
countDataReps(typing_data, tpx, dr_matches)

avgsFromDM(tpx_dists, groups, g_cuts, type, cname, tpx)

# df has colnames Strain, T0, dr (in example):
sectionTypingData(df, maxsize)

sectionClusters(k, typing_data, m)

distMatrix(input_data, dtype, cnames)

collectDistances(k, m, parts, fpath1, fpath2)
